# finaxe — ⚠️ needs review

> **This repo bundles several unrelated projects** under one tree (`buyside/` equity-research service, `education/` + `Educations/` tooling, `Days/`, plus app scaffolding). It looks like a bulk/working-directory commit rather than a single product.

**Recommendation:** split the real project(s) out into their own repos (e.g. `buyside/` clearly stands alone), then make this monorepo private or archive it.
