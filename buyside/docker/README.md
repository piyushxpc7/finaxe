# Docker Configuration Files

This directory is designated for storing Dockerfiles, environment files, and helper scripts used for containerized deployment.

Currently, the primary configurations are maintained in the root directory:
- `Dockerfile`: Multi-stage build for production.
- `docker-compose.yml`: Local multi-container development environment.
